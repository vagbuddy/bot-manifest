---
name: database-schema
description: Aligns SQL and ORM code with real table and column metadata from the database or migrations. Use when writing queries, migrations, or data access layers.
---

# Database and schema

- Use available SQL or database tooling to inspect live schemas when writing or changing persistence code.
- Match **exact** column names and types from existing metadata or migrations—do not invent tables or columns.
